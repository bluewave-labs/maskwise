# Maskwise Environment Configuration

# Database Configuration
POSTGRES_DB=maskwise
POSTGRES_USER=maskwise
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_PORT=5432

# Redis Configuration
REDIS_PORT=6379

# Application Ports
API_PORT=3001
WEB_PORT=3000
NGINX_PORT=80
NGINX_SSL_PORT=443

# Presidio Services
PRESIDIO_ANALYZER_PORT=5003
PRESIDIO_ANONYMIZER_PORT=5004

# Tika Service
TIKA_PORT=9998

# JWT Configuration (REQUIRED - Must be set for production)
# Generate strong secrets with: openssl rand -base64 32
# NEVER use the same secret in production as in development
JWT_SECRET=your_jwt_secret_here_minimum_32_characters_CHANGE_THIS
JWT_REFRESH_SECRET=your_jwt_refresh_secret_here_minimum_32_characters_CHANGE_THIS

# Environment
NODE_ENV=production

# Frontend API URL
NEXT_PUBLIC_API_URL=http://localhost:3001

# Optional: External service URLs (if not using Docker Compose)
# DATABASE_URL=postgresql://user:password@host:5432/database
# REDIS_URL=redis://host:6379