# MaskWise Production Environment Configuration
# Copy this file to .env and fill in all required values

#=============================================================================
# REQUIRED - Must be set (no defaults for security)
#=============================================================================

# Database Configuration
POSTGRES_DB=maskwise
POSTGRES_USER=maskwise
POSTGRES_PASSWORD=                              # REQUIRED: Set strong password (min 16 chars)

# JWT Authentication Secrets
JWT_SECRET=                                     # REQUIRED: Minimum 32 characters, cryptographically secure
JWT_REFRESH_SECRET=                            # REQUIRED: Different from JWT_SECRET, min 32 chars

# Default Admin Account
DEFAULT_ADMIN_EMAIL=                           # REQUIRED: Your admin email (not admin@maskwise.com!)
DEFAULT_ADMIN_PASSWORD=                        # REQUIRED: Strong password (min 12 chars, mixed case, numbers, symbols)

# Application URLs
NEXT_PUBLIC_API_URL=                          # REQUIRED: Your API URL (e.g., https://api.yourdomain.com)
CORS_ORIGINS=                                 # REQUIRED: Your frontend URLs (e.g., https://yourdomain.com,https://www.yourdomain.com)

#=============================================================================
# Optional Configuration (has sensible defaults)
#=============================================================================

# Node Environment
NODE_ENV=production

# File Upload Limits
MAX_FILE_SIZE=104857600                       # 100MB in bytes

# Session Configuration
SESSION_TIMEOUT=3600                          # 1 hour in seconds
REFRESH_TOKEN_EXPIRY=604800                   # 7 days in seconds

# Logging Level
LOG_LEVEL=info                                # Options: error, warn, info, debug

# Feature Flags
ENABLE_UNLEARNING=false
ENABLE_CLOUD_CONNECTORS=true
ENABLE_SSO=false

#=============================================================================
# Security Recommendations
#=============================================================================

# Generate secure secrets:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# Example strong password policy:
# - Minimum 12 characters
# - Include uppercase, lowercase, numbers, and symbols
# - Don't use dictionary words or personal information
# - Use a password manager

# CORS Configuration:
# - List all domains that will access your API
# - Don't use wildcards (*) in production
# - Include both www and non-www versions if applicable